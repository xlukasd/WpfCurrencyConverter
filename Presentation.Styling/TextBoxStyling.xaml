<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:markup="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="AmountTextBoxStyle" TargetType="{markup:Type TextBox}">
        <Setter Property="FontSize" Value="24" />
        <Setter Property="Width" Value="300" />
        <Setter Property="BorderThickness" Value="3" />
        <Setter Property="Margin" Value="3" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border Background="White" BorderThickness="2" BorderBrush="Gainsboro" CornerRadius="5">
                        <ScrollViewer Background="Transparent" x:Name="PART_ContentHost"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>